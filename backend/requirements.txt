# Web framework
fastapi==0.133.0
uvicorn[standard]==0.30.0
python-multipart==0.0.22

# Settings and validation
pydantic==2.12.5
pydantic-settings==2.13.1

# HTTP client
httpx==0.28.1
tenacity==8.5.0

# Async Redis
redis==5.0.8

# Data processing
numpy>=2.4.0
pandas>=3.0.0
scipy>=1.17.0
lxml>=6.0.0  # yfinance get_earnings_dates (Ted gate 7) parses HTML via pandas.read_html

# Machine learning
xgboost==3.2.0
scikit-learn==1.8.0
joblib==1.5.3
optuna==4.7.0

# NLP / FinBERT
# NOTE: Install torch FIRST with the CPU wheel before running pip install -r requirements.txt:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers==5.2.0
tokenizers==0.22.2

# Market data (free)
yfinance==1.2.0
requests==2.34.2

# Schwab Market Data API (real-time options + broker greeks)
schwab-py>=1.2.0

# Cryptography (token encryption at rest)
cryptography==50.0.1  # was >=42.0.0; Tier-1 advisory patch incl. GHSA-537c (HANDOFF §8)

# Security-pinned transitives (Tier-1 advisory patches, 2026-09-21 — HANDOFF §8).
# Pulled transitively via authlib / yfinance / requests; pinned so a fresh venv
# does not reintroduce the vulnerable versions flagged by pip_audit.
urllib3==2.8.0
idna==3.20
authlib==1.8.0
# curl-cffi is capped at 0.13.0 by yfinance 1.2.0 (requires <0.14). There is no
# patched release below 0.14, so the advisory upgrade is deferred to a coordinated
# yfinance bump (same class as the deliberate starlette/fastapi hold). HANDOFF §8.
curl-cffi==0.13.0

# Utilities
python-dotenv==1.2.1
filelock==3.20.3
setuptools==78.1.1
structlog==24.4.0
ujson==5.11.0

# Rate limiting
slowapi==0.1.9

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
respx==0.21.1

# Dev tools
ruff==0.15.2
